

let initialState = {
    friends: [
        {id: 1, name: 'Diman',},
        {id: 2, name: 'Misha',},
        {id: 3, name: 'Andrey',},
    ],
};

const sidebarReducer = (state = initialState, action) => {
    return state;
}

export default sidebarReducer;